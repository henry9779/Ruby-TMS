<%= form_for task do |f| %>
  <%= f.label :title, t('task.title') %>
  <%= f.text_field :title %>
  <%= f.label :content, t('task.content') %>
  <%= f.text_area :content %>
  <%= f.submit t('button.submit')%>
<% end %>

<%= link_to t('link.back_root'), root_path %>
