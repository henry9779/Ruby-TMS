<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/"><%= t('task_management') %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= t('language') %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to '中文', :locale => 'zh-TW', class: 'dropdown-item' %>
          <div class="dropdown-divider"></div>
            <%= link_to 'English', :locale => 'en', class: 'dropdown-item' %>
        </div>
      </li>
    </ul>
  </div>

  <% if user_sign_in? %>
    <%= current_user.name %>
    <%= link_to t('log_out'), users_log_out_path, method: 'delete', class: 'nav-link' %>
  <% else %>
    <%= link_to t('sign_up'), users_sign_up_path, class: 'nav-link' %>
    <%= link_to t('log_in'), users_log_in_path, class: 'nav-link' %>
  <% end %>
</nav>
